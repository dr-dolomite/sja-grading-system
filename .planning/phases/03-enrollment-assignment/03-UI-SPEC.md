---
phase: 3
slug: enrollment-assignment
status: draft
shadcn_initialized: true
preset: "radix-nova / mist"
created: 2026-04-01
---

# Phase 3 — UI Design Contract

> Visual and interaction contract for Phase 3: Enrollment & Assignment.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | shadcn 4.1.1 | components.json |
| Style | radix-nova | components.json |
| Base color | mist | components.json |
| Component library | radix-ui 1.4.3 | CLAUDE.md |
| Icon library | lucide-react 1.7.0 | components.json |
| Font | Manrope (--font-sans, via next/font/google) | app/layout.tsx |
| CSS strategy | Tailwind v4 CSS-first, @theme inline in globals.css | CLAUDE.md |

Third-party registries: none declared. Registry vetting gate: not applicable.

---

## Spacing Scale

Declared values (multiples of 4 only):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, badge padding, inline chip spacing |
| sm | 8px | Form field internal padding, compact row gaps |
| md | 16px | Default element spacing, card padding, Sheet section gaps |
| lg | 24px | Section padding within tabs, filter bar spacing |
| xl | 32px | Layout gaps between page sections |
| 2xl | 48px | Major section breaks (tab content top padding) |
| 3xl | 64px | Page-level top/bottom padding |

Exceptions:
- Touch targets for icon-only action buttons (Edit, Remove in table rows): minimum 44px height. Use `h-11` (44px) for icon buttons to meet accessibility minimums.
- CSV import drop zone: minimum 96px height to be a visible drop target.

---

## Typography

All type uses Manrope (--font-sans). The project uses no second typeface for UI text.

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 14px (text-sm) | 400 (regular) | 1.5 | Table cell text, form help text, Sheet body copy |
| Label | 14px (text-sm) | 600 (semibold) | 1.4 | Form field labels, column headers, filter labels |
| Heading | 20px (text-xl) | 600 (semibold) | 1.2 | Page title, Sheet panel title, tab section titles |
| Display | 28px (text-2xl) | 600 (semibold) | 1.2 | Reserved — not used in this phase |

Rules:
- Table column headers: 14px / 600 / uppercase tracking-wide (text-sm font-semibold uppercase tracking-wide).
- Sheet titles: 20px / 600 using the shadcn Sheet header pattern.
- Empty state heading: 16px / 600 (text-base font-semibold).
- Badge / status chip text: 12px (text-xs) / 500 (medium).

---

## Color

Tokens reference CSS custom properties from app/globals.css. All values are oklch; equivalents listed for readability.

| Role | CSS Token | Approx Value | Usage |
|------|-----------|--------------|-------|
| Dominant (60%) | --background | oklch(1 0 0) — white | Page background, Sheet interior, table row background |
| Secondary (30%) | --card / --sidebar / --muted | mist light gray | Sidebar background, tab content area, filter bar background, hover row state |
| Accent (10%) | --primary | oklch(0.532 0.157 131.589) — green | Reserved for: primary CTA buttons only, active sidebar nav item, focus ring |
| Destructive | --destructive | oklch(0.577 0.245 27.325) — red-orange | Remove/delete action buttons and confirmation dialogs only |

Accent reserved for (explicit list):
1. "Enroll Student" primary CTA button
2. "Assign Teacher" and "Assign Adviser" Sheet submit buttons
3. "Import CSV" trigger button
4. Active navigation item in sidebar
5. Focus ring on interactive controls (--ring token, mist blue-gray)

Accent is NOT applied to: table rows, filter dropdowns, badges, status indicators, section headers.

Status badge colors (semantic, not accent):
- Assigned adviser: `bg-primary/10 text-primary` (green tint) — "Assigned"
- Unassigned adviser: `bg-muted text-muted-foreground` — "Unassigned"
- JHS tag: `bg-secondary text-secondary-foreground`
- SHS tag: `bg-secondary text-secondary-foreground`
- Active enrollment: `bg-primary/10 text-primary`

---

## Page & Navigation Structure

### Sidebar Navigation (source: 03-CONTEXT.md D-09, D-12)

Add one nav item to app-sidebar.tsx:

| Label | Route | Visible to |
|-------|-------|------------|
| Enrollment & Assignments | /dashboard/enrollment | ADMIN, PRINCIPAL |

### Page Layout

Single page at `/dashboard/enrollment` with two tabs (shadcn Tabs component, consistent with Phase 2 pattern):

| Tab | Label | Description |
|-----|-------|-------------|
| 1 | Students | Student profiles, creation, CSV import, filter by grade level + section |
| 2 | Assignments | Teacher-to-subject-section and adviser-to-section assignment tables |

Default active tab: Students.

---

## Component Inventory

All components are already installed (Phase 1 & 2). No new shadcn components required.

| Component | File | Usage in Phase 3 |
|-----------|------|-----------------|
| Tabs | components/ui/tabs.tsx | Students / Assignments tab switcher |
| Table | components/ui/table.tsx | Student list, teacher assignment table, adviser assignment table |
| Sheet | components/ui/sheet.tsx | Create Student, Edit Student, Assign Teacher, Assign Adviser, CSV Import preview |
| Select | components/ui/select.tsx | Grade level filter dropdown, section filter dropdown, teacher/adviser picker in assignment Sheet |
| Input | components/ui/input.tsx | Search bar (global student search), form fields in Create/Edit Student Sheet |
| Label | components/ui/label.tsx | Form field labels |
| Field | components/ui/field.tsx | Form field wrappers with validation error display |
| Badge | components/ui/badge.tsx | Assigned/Unassigned status, JHS/SHS tags |
| Button | components/ui/button.tsx | All actions (primary CTA, ghost row actions, destructive confirmation) |
| Checkbox | components/ui/checkbox.tsx | Row selection for bulk CSV import review |
| Sonner | components/ui/sonner.tsx | Toast notifications for all mutations |
| Skeleton | components/ui/skeleton.tsx | Loading state for tables while data fetches |
| Separator | components/ui/separator.tsx | Section separators inside Sheet panels |
| Tooltip | components/ui/tooltip.tsx | Icon-only button labels (Edit, Remove icons in table rows) |

---

## Interaction Contracts

### Students Tab

**Filter Bar (above table):**
- Grade level Select (leftmost) — options: G7 through G12
- Section Select (second) — options filtered by selected grade level; disabled until grade level is chosen
- Global search Input (rightmost, grows to fill remaining space) — searches student name or LRN across all sections regardless of filter state
- "Enroll Student" Button (primary, rightmost) — opens Create Student Sheet
- "Import CSV" Button (outline variant, adjacent to Enroll button) — opens CSV Import Sheet

**Student Table columns:**
1. LRN (sortable)
2. Name — Last, First Middle (sortable)
3. Grade Level + Section (e.g., "G8 — Charity")
4. Strand (SHS only; shows "—" for JHS)
5. Sex
6. Contact No. (shows "—" if blank)
7. Actions — Edit (pencil icon + tooltip), Remove (trash icon + tooltip, destructive)

Table row hover: `bg-muted` (mist light gray).

**Create Student Sheet:**
Fields (in order): LRN (required), Last Name (required), First Name (required), Middle Name (optional), Grade Level (required, Select), Section (required, Select — filtered by Grade Level), Strand (conditional — shown only when G11 or G12 selected), Sex (required, Select: Male / Female), Contact Number (optional)

Auto-enrollment note (display in Sheet below section field, 12px muted text): "Student will be automatically enrolled in all subjects for the selected section."

Submit button label: "Enroll Student" (primary)
Cancel button label: "Cancel" (outline)

**Edit Student Sheet:**
Same fields as Create. Submit button label: "Save Changes".

**CSV Import Sheet:**
Step 1 — Drop zone: "Drop your CSV file here or click to browse". Accepted format note: "Download template" link (downloads a CSV template with required columns).
Step 2 — Preview table: shows parsed rows with validation status per row (valid = green check badge, invalid = destructive badge with error reason). Column mapping is fixed to the provided template (no custom mapping UI in v1).
Step 3 — Import confirmation: "Import N students" primary button. Rows with errors are skipped with a count shown.
Error summary: "X rows will be skipped — [download error report]" link.

### Assignments Tab

**Teacher Assignments section:**

Section header: "Teacher Assignments" (20px / semibold)
Filter: Grade level Select (same pattern as Students tab)

Table columns:
1. Subject Name
2. Grade Level
3. Section
4. Assigned Teacher (shows full name, or "Unassigned" badge)
5. Actions — "Assign" / "Change" Button (outline, small)

Clicking "Assign" or "Change" opens Assign Teacher Sheet.

**Assign Teacher Sheet:**
Title: "Assign Teacher"
Fields: Subject (read-only display), Grade Level + Section (read-only display), Teacher (required, Select — lists all users with SUBJECT_TEACHER role, shows name + Employee ID)
Submit button label: "Assign Teacher" (primary)
Cancel: "Cancel" (outline)

**Adviser Assignments section:**

Section header: "Adviser Assignments" (20px / semibold)
Separator between Teacher Assignments and Adviser Assignments sections.

Table columns:
1. Grade Level
2. Section
3. Strand (shows "—" for JHS)
4. Current Adviser (shows full name, or "Unassigned" badge)
5. Actions — "Assign" / "Change" Button (outline, small)

Clicking opens Assign Adviser Sheet.

**Assign Adviser Sheet:**
Title: "Assign Adviser"
Fields: Section (read-only display), Adviser (required, Select — lists all users with ADVISER role, shows name + Employee ID)
Submit button label: "Assign Adviser" (primary)
Cancel: "Cancel" (outline)

---

## Copywriting Contract

### Students Tab

| Element | Copy |
|---------|------|
| Primary CTA | Enroll Student |
| Import CTA | Import CSV |
| Search placeholder | Search by name or LRN... |
| Grade level filter placeholder | Grade Level |
| Section filter placeholder | Section |
| Empty state heading | No students enrolled yet |
| Empty state body (no filter active) | Start by enrolling students one at a time or import a CSV file to onboard the whole class at once. |
| Empty state body (filter active, no results) | No students found in this section. Try a different grade level or section, or clear the search. |
| Global search no results | No students match your search. Check the spelling or try the LRN. |
| Error state (load failure) | Could not load students. Check your connection and try again. |
| Section auto-enroll note (in Sheet) | This student will be automatically enrolled in all subjects for the selected section. |
| CSV drop zone label | Drop your CSV file here, or click to browse |
| CSV template link | Download template |
| CSV import preview button | Import {N} Students |
| CSV skip notice | {N} rows will be skipped due to errors — Download error report |
| CSV import success toast | {N} students enrolled successfully |
| CSV import partial success toast | {N} students enrolled. {M} rows skipped — see error report. |
| Student created success toast | Student enrolled successfully |
| Student updated success toast | Changes saved |
| Student removed success toast | Student removed |

### Assignments Tab

| Element | Copy |
|---------|------|
| Teacher assignments section heading | Teacher Assignments |
| Adviser assignments section heading | Adviser Assignments |
| Unassigned badge label | Unassigned |
| Assign action (no current teacher) | Assign |
| Change action (teacher already assigned) | Change |
| Grade level filter placeholder | Grade Level |
| Empty state heading — teacher table | No subject assignments found |
| Empty state body — teacher table | Configure grade levels and subjects in School Structure first, then return here to assign teachers. |
| Empty state heading — adviser table | No sections found |
| Empty state body — adviser table | Add sections in School Structure before assigning advisers. |
| Error state (load failure) | Could not load assignments. Check your connection and try again. |
| Assign Teacher Sheet title | Assign Teacher |
| Assign Adviser Sheet title | Assign Adviser |
| Teacher assigned success toast | Teacher assigned successfully |
| Adviser assigned success toast | Adviser assigned successfully |

### Destructive Actions

| Action | Confirmation Approach | Confirmation Copy |
|--------|----------------------|-------------------|
| Remove student | Inline Sheet confirmation (not a separate dialog) — "Remove Student" button in the edit Sheet footer, shown only after initial save | Removing this student will also remove their enrollment records. This cannot be undone. Remove Student / Cancel |

No other destructive actions in this phase. Teacher and adviser assignment changes are non-destructive (overwrite, no history loss in v1).

---

## Loading & Error States

| State | Behavior |
|-------|----------|
| Table loading | Skeleton rows: 5 rows of `h-10` Skeleton blocks matching table column widths |
| Sheet submitting | Submit button shows loading spinner (shadcn Button loading state), form fields disabled |
| Filter change | Table replaces content immediately (RSC navigation); no separate loading indicator needed |
| CSV parsing | Inline progress inside CSV Import Sheet; "Parsing..." text while client processes file |
| Server Action error | Sonner toast: destructive variant with problem + action copy (see Copywriting Contract) |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | sheet, table, select, input, label, badge, button, checkbox, sonner, skeleton, separator, tooltip, tabs | not required — all already installed from Phase 1/2 |

No third-party registries declared. Registry vetting gate: not applicable for this phase.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
