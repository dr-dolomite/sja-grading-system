---
quick_id: 260406-d24
type: quick
description: "Add Export CSV button to Students tab — exports current students as CSV or blank template if empty"
files_modified:
  - components/enrollment-tabs.tsx
---

<objective>
Add an Export CSV button to the Students tab header. When clicked, it generates and downloads a CSV file:
- If students exist: CSV with header row + student data rows
- If no students: CSV with just the header row (acts as a blank template)

Headers match the import template: lrn,lastName,firstName,middleName,gradeLevel,sectionName,strand,sex,contactNumber
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add Export CSV button and download function to enrollment-tabs.tsx</name>
  <files>components/enrollment-tabs.tsx</files>
  <action>
Add a client-side `handleExportCsv` function and a DownloadIcon button next to Import CSV.
The function builds CSV content from the students array, creates a Blob, and triggers download.
  </action>
</task>

</tasks>
