---
quick_id: 260406-d24
status: complete
---

# Quick Task 260406-d24: Export CSV Button

## What was done
Added an "Export CSV" button to the Students tab header in `components/enrollment-tabs.tsx`.

- Button appears next to "Import CSV" with a DownloadIcon
- `handleExportCsv()` builds CSV content client-side from the students array
- Headers match the import template: `lrn,lastName,firstName,middleName,gradeLevel,sectionName,strand,sex,contactNumber`
- If students exist: downloads `students-export.csv` with header + data rows
- If no students: downloads `student-import-template.csv` with header only (acts as blank template)
- CSV values containing commas are escaped with double quotes

## Files modified
- `components/enrollment-tabs.tsx` — added `DownloadIcon` import, `handleExportCsv` function, Export CSV button

## Verification
- No new TypeScript errors
- No new ESLint errors
